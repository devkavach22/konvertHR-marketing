    // // src/api/authService.ts

    // interface LoginResponse {
    //   token: string;
    // }

    // export const loginUser = async (user_name: string): Promise<LoginResponse> => {
    //   try {
    //     const response = await api.post("auth", { user_name });
    //     return response.data;
    //   } catch (error: any) {
    //     console.error("❌ Login API Error:", error);
    //     throw error;
    //   }
    // };
